import companies from './companies';
import serviceStations from './stations';
import staff from './staff';
import shifts from './shifts';

const mockData = {
  companies,
  serviceStations,
  staff,  // This should be the full staff object
  shifts
};

export default mockData;